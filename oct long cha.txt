#include<bits/stdc++.h>
//#include<boost/multiprecision/cpp_int.hpp>
#define fast ios::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL)
using namespace std;
//using boost::multiprecision::cpp_int;
typedef long long LL;
typedef long double LD;
const int inf=0x3f3f3f3f;
const int infi=1e9+7;
const long int number=1e12;
int t,n;
LL sum,sum1;
string s;
int main()
{
    int k=0,q=0,flag=0,rock=0,max=0,countf=0;
    fast;
    cin>>t;
    if(t>=1&&t<=200)
    {
        while(t--)
        {
            sum=0;
            q=0;
            flag=0;
            cin>>n;
            pair<int,string> abc[n];
            
            
            for(int i=0;i<n;i++) 
            {
                cin>>abc[i].first;
                cin>>abc[i].second;
            } 
            
            int arr[n][50]={0};
            for(int i=0;i<n;i++)
            {
                s=abc[i].second;
                max=0;
                if(abc[i].first==-1){
                for(int j=0;j<(abc[i].second).length();j++)
                    {
                        if(s[j]>9)
                        s[j]=s[j]-55;
                        if((abc[i].second)[j]>max)
                        max=s[j];
                    }
                arr[i][0]=max;
                cout<<max<<endl;
                }
                else
                arr[i][0]=abc[i].first;
            }
            
            
            for(int i=0;i<n;i++)
            {
                flag=0;
                sum=0;
                q=0;
                s=abc[i].second;
                if(abc[i].first!=-1)
                {
                    for(k=s.length()-1;k>=0;k--)
                    {
                        if(s[k]>9)
                        s[k]=s[k]-55;
                        sum+=s[k]*pow(abc[i].first,q++);
                        if(sum>number)
                        {
                            cout<<-1<<endl;
                            flag=1;
                            break;
                        }
                    }
                    if(flag==0)
                    {
                        cout<<sum<<endl;
                      arr[i][abc[i].first]=sum; 
                    }
                }
                else
                {
                for(int j=arr[i][0]+1;j<37;j++)
                {
                    flag=0;
                    sum=0;
                    q=0;
                    for(k=s.length()-1;k>=0;k--)
                    {
                        if(s[k]>9)
                        s[k]=s[k]-55;
                        sum+=s[k]*pow(j,q++);
                        if(sum>number)
                        {
                            cout<<-1<<endl;
                            flag=1;
                            break;
                        }
                    }
                    if(flag==0)
                    {
                        arr[i][j]=sum;
                    }
                    if(flag==1)
                    break;
                }
                }
            }
        }
    }
}